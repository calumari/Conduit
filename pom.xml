<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>net.socialhangover.conduit</groupId>
    <artifactId>conduit-parent</artifactId>
    <version>dev-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Conduit (Parent)</name>

    <repositories>
        <repository>
            <id>aikar</id>
            <url>https://repo.aikar.co/content/repositories/aikar/</url>
        </repository>
    </repositories>

    <build>
        <defaultGoal>clean install</defaultGoal>
        <finalName>${project.artifactId}</finalName>
    </build>
    <modules>
        <module>Conduit-API</module>
        <module>Conduit-Server</module>
    </modules>
</project>
